{
  "name": "@studio/agent",
  "version": "0.1.0",
  "private": true,
  "description": "MVDA cut agent (Phase C): claims edit_session_requested videos, authors the EDD in a see→judge→adjust loop (Claude Agent SDK + in-process MCP over @studio/core edd-ops), and lands the video at the CUT gate. Runs on the render-farm environment (GitHub Actions).",
  "scripts": {
    "agent-queue": "tsx src/agent-queue.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.207",
    "@remotion/bundler": "4.0.290",
    "@remotion/renderer": "4.0.489",
    "@studio/core": "workspace:*",
    "@studio/render": "workspace:*",
    "@supabase/supabase-js": "^2.49.4",
    "remotion": "4.0.290",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.3",
    "typescript": "^5"
  }
}
